pre-commit:
  parallel: true
  jobs:
    - name: biome
      run: pnpm biome check --staged --no-errors-on-unmatched
    - name: typecheck
      run: pnpm astro check

commit-msg:
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit {1}
