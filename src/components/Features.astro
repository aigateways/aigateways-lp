---
const features = [
	{
		title: "セキュリティエンジニア対応",
		description: "現役のセキュリティエンジニアが直接対応。専門知識に基づいた最適なセキュリティ設定を提供します。",
		icon: "shield",
	},
	{
		title: "セキュリティDD",
		description: "環境構築後のセキュリティデューデリジェンスを実施。脆弱性の発見と対策を徹底的に行います。",
		icon: "search",
	},
	{
		title: "サポート体制",
		description: "導入後のサポートも充実。トラブル発生時の迅速な対応と、継続的なセキュリティアップデートを提供します。",
		icon: "phone",
	},
];

const iconClass = "w-7 h-7";
const iconStroke = "1.5";
---

<section id="features" class="py-28 sm:py-36 bg-slate-50 relative overflow-hidden">
	<div class="absolute bottom-0 left-0 w-96 h-96 bg-brand/5 rounded-full blur-[150px]" aria-hidden="true"></div>
	<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative">
		<div class="max-w-3xl mx-auto text-center mb-20" data-animate="fade-up">
			<div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-brand-200 bg-brand-50 mb-6">
				<span class="text-xs font-bold tracking-widest text-brand uppercase">Features</span>
			</div>
			<h2 class="text-3xl sm:text-4xl lg:text-5xl font-extrabold text-slate-900 tracking-tight">
				選ばれる<span class="text-brand">理由</span>
			</h2>
			<p class="mt-6 text-lg text-slate-500 leading-relaxed">
				セキュリティと信頼で、安心の業務環境を
			</p>
		</div>
		{/* スマホ: 1列で縦並び（サービス内容と同様） / タブレット以上: 3列グリッド */}
		<div class="grid grid-cols-1 md:grid-cols-3 gap-6" data-animate="fade-up" data-animate-stagger>
			{
				features.map((feature) => (
					<div class="group relative bg-white rounded-3xl border border-slate-200 hover:border-brand/30 p-8 sm:p-10 transition-all duration-500 hover:shadow-2xl hover:shadow-brand/5 hover:-translate-y-1 overflow-hidden">
						<div class="absolute inset-0 bg-gradient-to-br from-brand-50/0 to-brand-50/0 group-hover:from-brand-50/50 group-hover:to-emerald-50/30 transition-all duration-500"></div>
						<div class="relative z-10">
							<div class="w-14 h-14 rounded-2xl bg-brand-50 text-brand flex items-center justify-center mb-6 group-hover:bg-brand group-hover:text-white transition-all duration-300 group-hover:scale-110">
								{feature.icon === "shield" && (
									<svg class={iconClass} fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={iconStroke} stroke-linecap="round" stroke-linejoin="round">
										<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
									</svg>
								)}
								{feature.icon === "search" && (
									<svg class={iconClass} fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={iconStroke} stroke-linecap="round" stroke-linejoin="round">
										<circle cx="11" cy="11" r="8" />
										<path d="m21 21-4.35-4.35" />
									</svg>
								)}
								{feature.icon === "phone" && (
									<svg class={iconClass} fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={iconStroke} stroke-linecap="round" stroke-linejoin="round">
										<path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z" />
									</svg>
								)}
							</div>
							<h3 class="text-xl font-bold text-slate-900 mb-4">{feature.title}</h3>
							<p class="text-slate-500 leading-relaxed text-[15px]">{feature.description}</p>
						</div>
					</div>
				))
			}
		</div>
	</div>
</section>
