---
import Layout from "@/layouts/Layout.astro";
const base = import.meta.env.BASE_URL || "/";
---

<Layout title="送信完了 | OpenClaw" description="お問い合わせを受け付けました。">
	<div class="min-h-screen bg-[#1A1E27] flex items-center justify-center px-4">
		<div class="max-w-md w-full text-center">
			<div class="w-16 h-16 rounded-full bg-brand/20 flex items-center justify-center mx-auto mb-6">
				<svg class="w-8 h-8 text-brand" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
					<path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
				</svg>
			</div>
			<h1 class="text-2xl font-bold text-white mb-2">送信が完了しました</h1>
			<p class="text-slate-400 mb-8">お問い合わせいただきありがとうございます。内容を確認のうえ、ご連絡いたします。</p>
			<a
				href={base}
				class="no-underline inline-flex items-center justify-center gap-2 px-8 py-4 text-base font-semibold text-white bg-brand hover:bg-brand-dark rounded-xl transition-colors"
			>
				トップへ戻る
				<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
			</a>
		</div>
	</div>
</Layout>
